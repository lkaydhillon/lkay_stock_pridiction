# lkay_stock_pridiction

>>> Remainings folders other than the existing file are:

           1 .  venv (which can be created using:
                                                  "python -m venv venv_name" 
                       & can be activated using:
                                                  "venv_name\Scripts\activate"

           2.  templates (move "index.html" file in this folder)

           3.  Static

Note:  there is no use of "Evolution.ipynb" as it is used to create the pickle files which contains the machine learning models.

>>> There are two ML models already created using the historical price of IOCL company. And these models are used to predict the price.
